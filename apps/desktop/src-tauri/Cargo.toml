[package]
name = "sthyra-desktop-shell"
version = "0.1.0"
description = "Native shell for NyraQ"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
